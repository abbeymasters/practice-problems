
const generateTheString = function(n) {
  // check if n % 2 === 0 and if so subtract one
  // loop through and add characters to equal n - 1
  // add one extra letter

  
};

export default generateTheString;
